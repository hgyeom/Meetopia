const initialState = [
  {
    userid: "aaa@aaa.com",
    nickname: "닉네임",
    password: "12341234",
    profileImg: "없",
  },
];

const users = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default users;
